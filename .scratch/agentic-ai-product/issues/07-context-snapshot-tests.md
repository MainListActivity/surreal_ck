Status: ready-for-agent
Label: ready-for-agent

# AI-007 — AI 上下文快照：shared 测试套件 + Web 选择态接线

> *This was generated by AI during triage.*
>
> 2026-06-10 重写：原 issue 指向 pre-pivot 的 `src/shared/ai-context.ts` 与 Electrobun renderer。快照构建函数现已迁到 shared 包（`buildAiContextSnapshot`），但**零测试、web 零调用**——D2-08 的 MVP 只发最小 snapshot `{ workspaceSlug, route }`，完整快照接线正是 D2-08 notes 里点名的"未来 issue"，即本 issue。

## Parent

`.scratch/agentic-ai-product/PRD.md`

## Blocked by

- 接线半边 blocked by `.scratch/web-frontend-migration/issues/08-ai-drawer-stream.md`；**测试半边无阻塞，可立即开工**。

## Agent Brief

**Category:** enhancement
**Summary:** 给 shared 的 `buildAiContextSnapshot` 补齐行为测试；AI 抽屉发消息时从编辑器选择态构建完整快照随消息提交。

**Current behavior:**
`buildAiContextSnapshot` 与 `AiContextSnapshot` 类型在 shared 包中存在但没有任何测试；web 端没有调用者，`/api/chat` 消息只携带最小 snapshot，行分析等依赖选中行的能力拿不到 record id。

**Desired behavior:**
1. **测试半边（无阻塞）**：覆盖四种选择状态的快照构建——no-selection（无 workbook/sheet/selectedRow）、workbook-only、sheet-selected、row-selected（含 label 与可见值）；陈旧态清除（切 sheet 清 selectedRow、切 workbook 清 sheet+selectedRow、取消选行退回 sheet-selected）；stable identifier 规则（label 按 name > code > id 优先级拼接；id 用 RecordId 字符串形态）。
2. **接线半边（D2-08 后）**：抽屉发消息时由 editor store 当前选择态构建快照并放进 `/api/chat` 请求体；抽屉头部展示上下文提示（`工作簿 / Sheet` 或 `Sheet / 主要字段组合`），提示仅供用户确认，提交的结构化快照才是 canonical 输入；选择变化时提示同步更新，不发送陈旧快照。

**Key interfaces:**
- `AiContextSnapshot` / `BuildAiContextSnapshotInput` / `buildAiContextSnapshot`（shared）——行为合约以现有实现为准，测试只断言外部可见输出，不锁内部结构。
- `/api/chat` 请求体的 contextSnapshot 字段——已有 DTO，不改形状。
- editor store 的选择态（当前 workbook / sheet / 选中行）——接线半边的输入来源。

**Acceptance criteria:**
- [ ] 四种选择状态各有独立测试，断言快照字段存在性与取值。
- [ ] 切 sheet / 切 workbook / 取消选行的陈旧态清除均有测试。
- [ ] label 优先级测试：name 存在时不落到 code 或 id。
- [ ] 全部测试无网络、无 LLM 依赖（纯函数输入输出）。
- [ ] （D2-08 后）抽屉消息携带由真实选择态构建的快照；选中行时快照含 record id 与可见值。
- [ ] （D2-08 后）上下文提示随选择变化更新，发送的快照与提示一致。

**Out of scope:**
- 快照字段瘦身 / sidecar 字段裁剪（独立优化，不混入本 issue）。
- 服务端如何消费快照（Router workflow 已有合约）。
