# ADR: workspace 映射为独立 SurrealDB database，Workspace Scope Module 管理进入范围

- **Status**: Accepted
- **Date**: 2026-05-16
- **Scope**: `schema/`、`server/`、`shared/sql/**`、所有跨 workspace / 跨 db 操作、SurrealDB Access 配置
- **Companions**:
  - [`web-only-pivot.md`](./web-only-pivot.md)（部署形态）
  - [`frontend-direct-connect.md`](./frontend-direct-connect.md)（2026-05-17 决定：前端直连 SurrealDB，Workspace Scope Module 管理 token scope）
  - [`virtual-office.md`](./virtual-office.md)（业务能力）

> **2026-05-17 update**：IdP 不再被视为 workspace 列表和成员关系权威。IdP 只负责签发 OIDC token；workspace 列表、最近一次登录 workspace、workspace 创建和成员索引由本应用的 **Workspace Scope Module** 维护。
>
> **2026-07-12 implementation clarification**：当前 token 使用短 claim `db` / `ac` / `RL`。IdP 每次正常签发前调用本应用 hook 获取 `db` / `ac`，client 配置为正常登录固定 `RL=["Owner"]`；scope exchange 当前随 `ac` 显式重申 `RL`。`ac=admin` 进入 JWT system access，Owner role 生效并获得 DDL；`ac=participant` 进入 RECORD access，忽略任何 system role、只受 PERMISSIONS 约束。`_system.system_admin` 非空表示部署级 workspace 创建开关开启，**不是逐 subject allowlist**。

## Context

[`web-only-pivot.md`](./web-only-pivot.md) 接受了"Web 前端 + Bun server + SurrealDB 公网 WSS"形态。

之前若干稿仍假定单一 SurrealDB database 承载全部 workspace 的数据，依赖：

- 在每张表上写复杂 PERMISSIONS 来跨 workspace 隔离。
- 后端持有长期 service JWT 代所有用户写动态表。
- workspace 创建和用户 DDL 操作都必须由后端 root 模板中转。

这套做法在新部署形态下被几个事实持续推翻：

1. **每 workspace 一个 database 是 SurrealDB 自然提供的隔离单元**。跨 workspace 隔离不再需要在每张业务表里表达。
2. **SurrealDB access 能表达 admin / participant / employee 三类执行能力**。DDL 与 DML 能力应由 DB 引擎硬隔离。
3. **业务数据读写应该以真实用户会话执行**。写入归因天然在 `$auth`，不需要 service JWT 和 `from_*` 代写字段。
4. **workspace scope 是本应用业务，不是 IdP 业务**。IdP 只签 token；本应用决定用户能进入哪个 workspace、以什么 access 进入。

## Decision

### 1. Topology

SurrealDB namespace `main` 下有：

| Database | 职责 |
|---|---|
| `_system` | root-only system database，承载 `workspace`、`user_workspace_index`、系统迁移版本等跨 workspace 元数据。 |
| `ws_<id12>` | 每个 **工作区** 一个 workspace database，承载该工作区全部业务表、协作表、`user` 表和 access 定义。 |

### 2. Token scope claim

真人 OIDC token 中使用三个 SurrealDB claim：

| Claim | 来源 | 含义 |
|---|---|---|
| `db` | 本应用 hook / scope exchange | 当前 workspace database，例如 `ws_a1b2c3d4e5f6`。 |
| `ac` | 本应用 hook / scope exchange | 当前 access：`admin` 或 `participant`。 |
| `RL` | IdP client 固定配置；scope exchange 也显式重申 | `['Owner']`；只在 JWT system access 中生效，RECORD access 不使用 system RBAC。 |

浏览器用 `db` / `ac` 选择 SurrealDB access；`RL` 决定 JWT system access 的数据库角色：

```
namespace: main
│
├── database: _system                     — root-only：无 access 定义，仅后端 root 连接可访问
│     ├── TABLE workspace                — workspace 索引（本应用权威；由 Workspace Scope Module 写）
│     │     • db_name (string, unique)    — 该 workspace 的 db 名
│     │     • owner_subject (string)      — 创建者的 OIDC subject
│     │     • slug (string, unique)       — URL 展示用
│     │     • name (string)
│     │     • status (string)             — 'provisioning' | 'active' | 'failed' | 'archived'
│     │     • last_migration_version (option<string>) — 已应用到本 db 的 workspace-template 最高版本号
│     │     • last_migration_error (option<string>)   — 上一次迁移失败原因（status='failed' 时填）
│     │     • created_at, updated_at, archived_at?
│     ├── TABLE user_workspace_index     — 本应用权威索引：subject → 可进入的 ws db
│     │     • subject (string, indexed)   — OIDC subject
│     │     • email (option<string>)      — 显示和首次认领辅助，不作为唯一权威
│     │     • workspace (record<workspace>)
│     │     • db_name (string)            — 冗余 workspace db 名，便于 IdP scope adapter 直接取
│     │     • role (string)               — 'admin' | 'participant'，映射到 SurrealDB access
│     │     • joined_at (datetime)
│     │     • last_selected_at (option<datetime>) — 用户最近一次进入 / 切换到该 workspace 的时间
│     │     • last_seen_at (option<datetime>)     — 本应用观察到该 subject 最近活跃时间
│     │     • disabled_at (option<datetime>)      — 被移除或禁用时填写；登录 hook 与列表过滤掉
│     │
│     │ 维护规则：
│     │   • 本应用是权威：workspace 创建、成员变更、最近选择、disable 全部经过 Workspace Scope Module 用 root 写。
│     │   • IdP 不写本表；IdP 每次签发时调本应用 default-scope hook 读取 scope。
│     │   • 登录 hook 选默认 scope；switch-workspace 校验 membership。
│     │   • Reconciler 周期性校对本表与各 ws db 的 user 表是否漂移（参见 §8）。
│     └── TABLE system_admin             — 历史物理名；非空即开启部署级 workspace 创建 capability
│           • 不是逐 subject allowlist；subject 只用于 seed / 审计
│
└── database: ws_<auto-generated-id>      — 每 workspace 一个 db，db 名由系统自动生成（见 §2.1）
      ├── TABLE user                      — 该 workspace 的所有人（管理员、普通成员、虚拟员工共表）
      │     • email (string, unique)      — 真人定位锚点（管理员添加成员时输入此项）
      │     • subject (option<string>, unique) — 真人首次 OIDC 登录时被 AUTHENTICATE 回填；虚拟员工创建时即填 urn:virtual:<uuid>
      │     • kind (string)               — 'human' | 'virtual'
      │     • is_admin (bool)             — 仅 human 用；区分管理员 / 普通成员
      │     • display_name, avatar
      │     • last_seen_at (option<datetime>) — NONE 表示"从未登录"；每次 SIGNIN 在 AUTHENTICATE 内更新
      │     • virtual_profile (object?)   — 仅 virtual 时填，含 role / supervisor / status
      ├── TABLE workbook / sheet / edge_catalog / dashboard_page / ...
      ├── TABLE office_role / office_task / office_message / office_report / user_notification / employee_credential
      ├── TABLE ent_* / rel_*             — 动态业务表
      │
      │  ── 三条 ACCESS：身份隔离落在 DB 引擎层，不依赖应用层兜底 ──
      │
      │── DEFINE ACCESS admin ON DATABASE TYPE JWT URL '<oidc-jwks>'
      │     • hook 决定 ac=admin；固定 RL=['Owner'] 作为 database system role 生效
      │     • DDL + DML；不受 record PERMISSIONS 限制
      │     • $auth=NONE；fn::current_user() 按 $token.sub 解析管理员 user record
      │
      │── DEFINE ACCESS participant ON DATABASE TYPE RECORD WITH JWT URL '<oidc-jwks>'
      │     • hook 决定 ac=participant；token 中任何 RL 都不作为 RECORD user 权限
      │     • 无 DDL；DML 只按表与字段 PERMISSIONS 放行
      │     • $auth 指向 workspace database 内的 user record
      │
      └── DEFINE ACCESS employee ON DATABASE TYPE RECORD
            • dispatcher 用员工 secret SIGNIN
            • 无 DDL；DML 只按表与字段 PERMISSIONS 放行
            -- （SIGNIN 是定义在 db 级的特权脚本，实测可读 PERMISSIONS NONE 表）。
```

### 3. 三类 workspace 身份

| 身份 | user 表记录 | 走的 access | DB 引擎能力 | 谁持 token |
|---|---|---|---|---|
| **工作区管理员** | `kind='human', is_admin=true` | `admin` ON DATABASE (TYPE JWT) | DDL + DML | 浏览器 |
| **普通成员** | `kind='human', is_admin=false` | `participant` ON DATABASE (TYPE RECORD WITH JWT) | 仅 DML | 浏览器 |
| **虚拟员工** | `kind='virtual'` | `employee` ON DATABASE (TYPE RECORD) | 仅 DML | dispatcher 用员工 secret SIGNIN |

不再存在 NS-admin token。workspace 创建由后端 Workspace Scope Module 用 root 完成，浏览器不持跨 db DDL 能力。

AI / 虚拟员工发起的 DDL 不改变上表能力划分：虚拟员工仍不能直接 DDL。DDL 的权限来自发出指令的真人用户，必须使用该用户当前 workspace session 执行；如果发起人不是工作区管理员，SurrealDB admin access 会拒绝。后端不得用 root、service JWT 或员工 session 代写业务 DDL。

### 4. `_system` database 职责

`_system` root-only，真人和虚拟员工都没有 access。它承载：

| 表 | 用途 |
|---|---|
| `workspace` | workspace 索引；字段集见 §2 拓扑图（含 `status`、`last_migration_version` 等）。 |
| `user_workspace_index` | 本应用维护的 subject → workspace 映射；字段集见 §2 拓扑图。 |
| `_system_schema_version` | `_system` 自身迁移版本。 |

> 字段集以 §2 拓扑图为单一来源；新增字段时只改 §2，不在本节复述。

`user_workspace_index` 是应用权威索引，由 Workspace Scope Module 和 reconciler 共同维护，用于：

- IdP 登录 hook 选择默认 token scope。
- 前端登录后列出可进入 workspace。
- Workspace Scope Module 验证切换目标。
- dispatcher 启动时枚举要服务的 workspace。

`workspace.status` 取值语义：

| 取值 | 含义 |
|---|---|
| `provisioning` | 创建流程进行中：db 已创建但模板 / owner / IdP scope 切换尚未全部完成。前端登录 / 列表不显示。 |
| `active` | 全部 lifecycle 步骤成功，可登录、可调度、可迁移。 |
| `failed` | lifecycle 中途失败；`last_migration_error` 写明原因。等待管理员人工处置（重试 / 删除）。 |
| `archived` | 已归档，登录与 dispatcher 都跳过；保留数据以便恢复。 |

> Mastra `WorkflowsStorage` snapshot **不**落在 `_system`——它跨 workspace 会破坏 db 边界隔离。snapshot 应落在所属 workspace db 内（参见簇 D1 落地）。

### 5. Workspace Scope Module

后端提供一个深 Module，隐藏 `_system` 查询、workspace db user 校验、IdP scope 更新、workspace 创建补偿等 Implementation。它的 Interface 只暴露：

- 列出当前 OIDC subject 可进入的 workspace。
- 切换 workspace：校验 membership，更新 `last_selected_at`，调用 IdP Token Scope Adapter。
- 给 IdP 登录 hook 返回默认 scope。
- 创建 workspace：root 建 db、应用模板、写 owner、写 `_system` 索引、切 token scope。

删除这个 Module 后，复杂度会重新散落到前端、IdP hook、workspace 创建流程和 dispatcher，因此它是有 Depth 的 Module。

### 6. 登录与切换

登录时，IdP 调本应用 default-scope hook。本应用按 subject 查 `user_workspace_index`：

1. 优先选择 `last_selected_at` 最新且未 disabled 的 workspace。
2. 如果没有最近选择，选择第一个 active workspace。
3. 如果没有任何可进入 workspace：workspace 创建开关开启则返回 `_system` scope 承接创建流程；关闭则拒绝登录。

切换时，前端调本应用 `POST /api/session/switch-workspace`。后端必须验证：

- 当前 OIDC subject 存在于 `_system.user_workspace_index` 且未 disabled。
- 目标 workspace database 中也有对应 human `user` 记录，或能按 email/subject 完成一致性修复。
- 得出的 access 与 workspace 内 `user.is_admin` 一致。

验证通过后，后端更新 `last_selected_at`，再调用 IdP Token Scope Adapter 换发带目标 `db` / `ac` 的 token（当前 Adapter 同时显式重申对应 `RL`）。

### 7. Workspace 创建

workspace 创建由后端完成：

1. 验证当前 OIDC subject 有创建 workspace 权限。
2. 生成 `db_name = ws_<id12>`；用户输入的 slug 只用于 URL 和 `_system.workspace.slug`。
3. root 创建 workspace database。
4. 应用 `shared/sql/workspace-template/` 全量模板。
5. 创建 owner user：`kind='human', is_admin=true`。
6. 写 `_system.workspace` 与 `_system.user_workspace_index`。
7. 调用 IdP Token Scope Adapter，把用户 scope 切到新 workspace 的 `admin` access。

失败补偿集中在 Workspace Scope Module：模板失败则删除刚创建的 db；IdP scope 更新失败则返回可重试错误，避免前端散落补偿逻辑。

### 8. 成员管理

MVP 成员模型仍然是"管理员直接预创建成员记录"：

- workspace database 内 `user` 表是该 workspace 的成员事实表。
- `_system.user_workspace_index` 是跨 workspace 登录 / 列表索引。
- 成员新增、移除、role 变更必须同时维护二者。

**写路径（2026-05-18 决定）**：成员管理 endpoint 归 Workspace Scope Module，由后端 root 原子同写 ws db `user` 表与 `_system.user_workspace_index`。前端不直接写 `user` 表的 human 行。

- `POST /api/workspaces/:slug/members`：邀请 / 添加成员。
- `PATCH /api/workspaces/:slug/members/:userId`：role 变更。
- `DELETE /api/workspaces/:slug/members/:userId`：移除（写 `disabled_at`，不真删；保留 user record 用于历史归因）。

Reconciler 周期性校对两边一致性：若发现 `_system.user_workspace_index` 与 ws db `user` 表漂移（例如管理员通过 SurrealDB 客户端绕过 endpoint 直写），按 ws db 为事实回填 `_system`，并打日志。

### 9. 后端连接管理

- 一条 root 连接到 `_system`：schema 初始化、workspace 索引、default scope、dispatcher 枚举。
- 按需 root 连接到 workspace db：workspace 创建、模板迁移、`employee_credential` 写入。
- Mastra chat：后端用调用者 OIDC token 按 token scope SIGNIN，workflow 结束后丢弃。
- Office dispatcher：每个执行窗口用 employee secret SIGNIN。
- 日常业务数据读写：浏览器直连 SurrealDB。

### 10. service JWT 概念取消

架构内只有：

- 浏览器真人会话。
- dispatcher employee 会话。
- root 维护路径。

不存在 service JWT，也不存在长期员工 token。所有业务写入归因都走 `$auth`。

## Consequences

### 正面

- **隔离天然**：跨 workspace 隔离由 database 边界保证。
- **IdP Interface 变窄**：只负责签发 / 更新 token scope。
- **workspace scope Locality 高**：默认 workspace、切换、创建、最近选择、scope 更新集中在一个 Module。
- **浏览器不持跨 db DDL 能力**：workspace 创建更容易补偿和审计。
- **新业务查询零后端发版**：前端写 SurrealQL，DB engine 把关。

### 负面 / 必须正面解决

- **`user_workspace_index` 维护必须严谨**：否则登录 hook、workspace 列表和 dispatcher 枚举会漂移。
- **成员管理需要明确写路径**：不能只写 workspace `user` 表而忘记 `_system`。
- **IdP scope 更新失败需要重试策略**。
- **SurrealDB 公网暴露**：必须配置 query timeout、connection limit、rate limit、TLS 和 WAF。
- **跨 db 报表**：MVP 不做；未来由后端 fan-out 或专门索引表实现。

## Alternatives Considered

### A. 单 db + 复杂 PERMISSIONS

抛弃。PERMISSIONS 嵌套深、跨 workspace 隔离全靠人工表达式、service JWT 攻击面大。

### B. IdP 作为 workspace 权威

抛弃。workspace 列表、成员关系和最近选择是本应用业务；IdP 只应该签 token scope。

### C. 浏览器 NS-admin 创建 workspace

抛弃。它把跨 db DDL 能力暴露给浏览器，且失败补偿分散。

### D. 每 workspace 一个 namespace

抛弃。namespace 之间管理和跨 workspace 索引更重；同 namespace 多 database 已足够表达本架构隔离。

## Open Questions

1. ~~**成员管理写路径**：全走后端 endpoint，还是前端写 workspace `user` 后调用后端同步 `_system`，待 issue 固化。~~ **已决定**（2026-05-18，见 §8）：全走 Workspace Scope Module 后端 endpoint，root 原子同写。
2. **IdP scope adapter 细节**：IdP 已选定 `o.maplayer.top/t/ck`（见 [`../oidc.md`](../oidc.md)），提供 scope 更新 API；具体协议待簇 C issue 03 实测回填。
3. **workspace 创建权限**：应用 entitlement、付费计划还是 IdP claim，待产品策略定。
4. **role 变更 token 滞后**：用户 role 改变后，已签发 token 在 DURATION 内可能仍旧；MVP 通过切换 / refresh 解决，高敏场景再加撤销能力。
