# ADR: workspace 映射为独立 SurrealDB database，DDL/DML 走用户会话直接执行

- **Status**: Accepted
- **Date**: 2026-05-16
- **Scope**: `schema/`、`server/`、所有跨 workspace / 跨 db 操作、SurrealDB Access 配置
- **Companions**:
  - [`web-only-pivot.md`](./web-only-pivot.md)（部署形态）
  - [`virtual-office.md`](./virtual-office.md)（业务能力）

## Context

[`web-only-pivot.md`](./web-only-pivot.md) 接受了"Web 前端 + 单容器 Bun server + SurrealDB（同机房内网）"形态。

之前若干稿仍假定**单一 SurrealDB database** 承载全部 workspace 的数据，依赖：

- 在每张表上写复杂的 PERMISSIONS 来跨 workspace 隔离（`workspace.owner = $auth OR workspace IN $auth<-has_workspace_member<-workspace`）。
- 后端持有一个长期 service JWT 代所有用户写动态表。
- 用户的 DDL 操作（建表、加字段）必须由后端 root 权限的 execTemplate 中转。

这套做法在新部署形态下被几个事实持续推翻：

1. **每 workspace 一个 db 是 SurrealDB 自然提供的隔离单元**——一旦走这条路，跨 workspace 隔离不再需要在 PERMISSIONS 里手写，db 边界天然保证。
2. **SurrealDB 的 RECORD access + AUTHENTICATE query 已经能精确表达"哪些用户能登录、能登录后是什么权限"**。把 admin/普通成员/虚拟员工都放在 db 内 user 表里、用字段 + AUTHENTICATE 区分，比"全局表 + 复杂 PERMISSIONS"清楚得多。
3. **DDL 自服务**——既然每个 workspace 是自己的 db，让管理员的 JWT 直接拥有 DDL 能力是最直白的；不再需要 root execTemplate 中转 90% 的 schema 变更。
4. **service JWT 概念不再必要**——所有写都以"用户身份"执行，归因天然在 `$auth`，不需要在表字段里手工标 `from_*`。

## Decision

### 1. 拓扑

```
namespace: main
├── database: _system
│     ├── TABLE workspace                — 全局 workspace 索引
│     │     • db_name (string)            — 该 workspace 的 db 名
│     │     • owner_subject (string)      — 创建者的 OIDC subject
│     │     • created_at, name, slug
│     ├── TABLE pending_workspace_member  — 邀请态（用户接受前可能尚无任何 db user 记录）
│     │     • email, workspace, role, invited_at
│     └── DEFINE ACCESS member ON DATABASE TYPE JWT URL <oidc-jwks>
│           — 凡是合法 OIDC 用户都能登录 _system，但只能 SELECT 自己被邀请/拥有的 workspace
│
└── database: ws_<auto-generated-id>      — 每 workspace 一个 db，db 名由系统自动生成（见 §2.1）
      ├── TABLE user                      — 该 workspace 的所有人（管理员、普通成员、虚拟员工共表）
      │     • subject (string, unique)    — 真人：OIDC subject；虚拟员工：urn:virtual:<uuid>
      │     • kind (string)               — 'human' | 'virtual'
      │     • is_admin (bool)             — 仅 human 用；区分管理员 / 普通成员
      │     • display_name, avatar
      │     • virtual_profile (object?)   — 仅 virtual 时填，含 role / supervisor / status
      ├── TABLE workbook / sheet / edge_catalog / dashboard_page / ...
      ├── TABLE office_role / office_task / office_message / office_report / user_notification
      ├── TABLE ent_* / rel_*             — 动态业务表
      │
      │  ── 三条 ACCESS：身份隔离落在 DB 引擎层，不依赖应用层兜底 ──
      │
      │── DEFINE ACCESS admin ON DATABASE TYPE JWT URL 'https://o.maplayer.top/t/ck/jwks.json'
      │     AUTHENTICATE {
      │       LET $u = SELECT * FROM user WHERE subject = $token.sub LIMIT 1;
      │       IF $u = NONE OR $u.kind != 'human' OR $u.is_admin != true {
      │         THROW "not an admin of this workspace";
      │       };
      │       RETURN $u;
      │     };
      │     -- SurrealDB JWT access 用户默认拥有 db owner 级能力，能执行全部 DDL
      │     -- （建表 / 加字段 / DEFINE ACCESS 等）。AUTHENTICATE 已硬卡"只有
      │     -- is_admin=true 的真人才能登录"，DDL 自服务因此安全且不依赖代码层校验。
      │
      │── 
        DEFINE ACCESS OVERWRITE participant ON DATABASE TYPE RECORD
          WITH JWT URL 'https://o.maplayer.top/t/ck/jwks.json'
          AUTHENTICATE {

            IF type::is_array($token.aud) AND $token.aud CONTAINS 'https://auth.maplayer.top'
              OR $token.aud = 'https://auth.maplayer.top'
            {
              IF !$token.sub { THROW 'Missing sub claim' };

              LET $user_id  = type::record('user', $token.sub);
              LET $existing = (SELECT VALUE id FROM ONLY $user_id);

              IF !$existing {
                LET $display_name =
                  IF $token.name               { <string>$token.name               }
                  ELSE IF $token.preferred_username { <string>$token.preferred_username }
                  ELSE IF $token['https://surrealdb.com/email']         { <string>$token['https://surrealdb.com/email']              }
                  ELSE                         { <string>$token.sub                };

                CREATE $user_id CONTENT {
                  display_name: $display_name,
                  email:        $token['https://surrealdb.com/email'],
                  subject:      $token.sub,
                  avatar:       IF $token.picture { <string>$token.picture } ELSE { NONE },
                  created_at:   time::now(),
                  updated_at:   time::now()
                };
              };

              -- 自动认领：将匹配邮箱的 pending_workspace_member 转换为正式 has_workspace_member 边。
              -- 在 AUTHENTICATE 块内以系统权限运行，绕过行级权限。
              IF $token['https://surrealdb.com/email'] {
                LET $pending = (
                  SELECT id, workspace, role FROM pending_workspace_member
                  WHERE email = <string>$token['https://surrealdb.com/email']
                );
                FOR $inv IN $pending {
                  LET $ws = $inv.workspace;
                  RELATE $ws->has_workspace_member->$user_id CONTENT {
                    role:      $inv.role,
                    joined_at: time::now()
                  };
                  DELETE $inv.id;
                };
              };

              RETURN $user_id;

            } ELSE {
              THROW 'not allowed'
            }

          } DURATION FOR TOKEN 1h, FOR SESSION NONE;
      │
      └── DEFINE ACCESS employee ON DATABASE TYPE RECORD
            SIGNUP NONE
            SIGNIN {
              LET $u = SELECT * FROM user
                WHERE subject = $subject AND kind = 'virtual' AND virtual_profile.status = 'active'
                LIMIT 1;
              IF $u = NONE { THROW "no such employee"; };
              LET $c = SELECT VALUE secret FROM employee_credential WHERE employee = $u.id LIMIT 1;
              IF $c != $secret { THROW "bad secret"; };
              RETURN $u;
            }
            DURATION FOR SESSION 1h;
            -- 同 participant：RECORD access 无 DDL。
            -- secret 来自 dispatcher 用 root 凭证读 employee_credential 后传入。
```

### 2. 用户身份与 access 选择

三类身份在 DB 引擎层做硬隔离。后端在用户 OIDC 验证通过后，按下面这张表选 SIGNIN 走哪条 access：

| 身份 | user 表记录 | 走的 access | DB 引擎能力 | $auth |
|---|---|---|---|---|
| **工作区管理员** | `kind='human', is_admin=true` | `admin` (TYPE JWT) | DDL + DML | user 记录 |
| **普通成员** | `kind='human', is_admin=false` | `participant` (TYPE RECORD) | 仅 DML（DDL 被引擎拒） | user 记录 |
| **虚拟员工** | `kind='virtual'` | `employee` (TYPE RECORD) | 仅 DML（DDL 被引擎拒） | user 记录 |

关键性质：

- **DDL 权限的硬隔离落在 SurrealDB 引擎层**：JWT access 用户默认 db owner 级，RECORD access 用户被引擎硬拒任何 DDL。代码层完全不用兜底校验"该用户能不能 DDL"。
- **三类身份共用一张 user 表**，PERMISSIONS / FK / `$auth` 都不需要 union 不同表。
- **后端登录流程**：用户带 OIDC token 调后端 `POST /api/sessions`；后端在 OIDC verify 后，先查 `_system.workspace` 拿到该用户拥有/参与的 workspace 列表，逐个 SIGNIN：
  - 优先尝试 `admin` access（用 OIDC token + URL JWKS 验证）；成功即管理员通道。
  - 否则尝试 `participant` access（用 subject 作为入参 SIGNIN）；成功即普通成员通道。
  - 都失败则该用户不属于该 workspace。
  后端把对应的短期 token 缓存在 session 内，后续 API 调用按需选用。
- **同一真人在不同 workspace 中可以是不同身份**：A 工作区是管理员，B 工作区是普通成员。这天然由各 ws db 内的 user.is_admin 决定。
- **虚拟员工的 SIGNIN 由 dispatcher 负责**：详见 [`virtual-office.md`](./virtual-office.md) §1。

### 2.1 workspace database 命名

db 名形如 `ws_<id12>`，其中 `<id12>` 由系统自动生成（建议 12 字符 base32 / nanoid，去除易混淆字符）。**用户输入的 slug 仅用于 URL 展示和 _system.workspace.slug 字段**，不进入 db 名。

理由：

- 杜绝 slug 注入 SQL 风险。
- 避免 slug 改名时 db 名跟随（db rename 在 SurrealDB 是一个重操作）。
- 不暴露内部命名给用户。

`_system.workspace.db_name` 记录系统生成的 db 名；前端永远引用 slug，后端自己查映射。

### 3. _system database 的职责

`_system` 只承载**真正必须跨 workspace 才能存在**的数据：

- `workspace` 索引（一个用户登录后要先知道自己有哪些 workspace、对应哪个 db）
- `pending_workspace_member` 邀请（被邀请者尚未加入任何 db，无法存到 ws db 里）

**不放**到 `_system`：

- 用户表（用户在每个 workspace db 里独立存）
- 任何业务数据
- schema_version（每个 ws db 自己版本化）

`_system` 的 `workspace` 表 PERMISSIONS：

```surql
DEFINE TABLE workspace SCHEMAFULL
  PERMISSIONS
    FOR select WHERE
      owner_subject = $token.sub                        -- 我是创建者
      OR id IN (                                         -- 或者我被邀请过且已接受（pending 已删）
        SELECT VALUE workspace FROM pending_workspace_member
        WHERE email = $token.email AND accepted_at = NONE
      ),
    FOR create, update, delete WHERE owner_subject = $token.sub;
```

注意这里用 `$token.sub` 而非 `$auth`——`_system` 的 access 只暴露 token 本身，不做 user 表查询（`_system` 没有 user 表）。

### 4. workspace 创建走 execTemplate（root 权限）

新增 `execTemplate` 名 `create_workspace`：

入参（来自后端 endpoint，已校验调用者是合法 OIDC 用户）：

```
{
  ownerSubject: string,
  ownerEmail: string,
  ownerDisplayName?: string,
  workspaceName: string,
  workspaceSlug: string,
}
```

模板执行（root 权限）：

1. 后端先生成 `db_name = 'ws_' + nanoid(12)`，并对 `_system.workspace.db_name` 做唯一索引兜底冲突重试
2. `DEFINE DATABASE <db_name>`
3. `USE DB <db_name>;`
4. `DEFINE ACCESS admin ON DATABASE TYPE JWT URL <oidc-jwks> AUTHENTICATE { ... };`
5. `DEFINE ACCESS participant ON DATABASE TYPE RECORD SIGNIN { ... };`
6. `DEFINE ACCESS employee ON DATABASE TYPE RECORD SIGNIN { ... };`
7. seed 全部业务表 schema（office_role / office_task / ... / workbook / sheet / edge_catalog / ...）
8. INSERT 第一条 user：`{ subject: ownerSubject, kind: 'human', is_admin: true, display_name: ownerDisplayName }`
9. 回到 `_system` db，INSERT 一条 workspace 记录：`{ db_name, owner_subject, name, slug }`

模板**只做这一件事**。其余任何动作都不走 execTemplate。

### 5. 后续所有 DDL/DML 走用户会话

- 用户的 OIDC token 通过后端 endpoint 进来；后端按 §2 的"按身份选 access"流程拿到对应 token，透传查询 / 写入。
- **管理员**：走 `admin` access，token 直接拥有 DDL 能力，直接执行 `DEFINE TABLE` / `DEFINE FIELD` 等。
- **普通成员**：走 `participant` access，token 在 DB 引擎层就被拒任何 DDL；DML 受 PERMISSIONS 约束。
- **虚拟员工**：走 `employee` access，同样无 DDL；DML 受 PERMISSIONS 约束；归因 `$auth = user 记录`。

### 6. 跨 workspace 查询的处理

用户登录后第一步：后端用其 token 在 `_system` 中查"你有哪些 workspace"，把列表返给前端。用户选一个 workspace 后，前端的所有后续请求带上 `?ws=<slug>`，后端据此决定连哪个 db。

无需 SurrealDB 跨 db SELECT 能力。架构清晰、不依赖特性版本。

### 7. 后端连接管理

- 一条到 `_system` 的连接（用户 token），用于 workspace 列表 / 邀请。
- 每个活跃 workspace 维护一个 db-level connection pool（按需创建）。
- 虚拟员工执行窗口：从 employee record 取 secret → SIGNIN 该 ws db → 短期 token 用完即弃。

### 8. service JWT 概念取消

之前几稿中的 `service JWT` 整体不再存在。所有 SurrealDB 写入要么是真人会话、要么是虚拟员工 RECORD access 会话。后端**不持有任何长期 SurrealDB 凭证**，除了启动 execTemplate 用的 root 凭证（仅来自环境变量、仅在 `create_workspace` 执行时使用）。

## Consequences

### 正面

- **隔离天然**：跨 workspace 隔离由 db 边界保证，不靠手写 PERMISSIONS。
- **PERMISSIONS 大幅简化**：每张业务表只需要表达"我对这条记录的具体角色"，不需要嵌套 workspace 归属判断。
- **DDL 自服务**：管理员直接改自己 workspace 的 schema，零中转。
- **审计天然**：每条记录的 changefeed `$auth` 字段就是真实操作者，无需在字段里手工归因。
- **service JWT 攻击面消失**：后端无长期凭证（root 凭证仅在 `create_workspace` 内使用，可短期化或借助密钥管理服务）。
- **虚拟员工身份原生**：SurrealDB RECORD access 直接表达，不需要额外签发服务。

### 负面 / 必须正面解决

- **db 数量增长**：SurrealDB 对单 namespace 内 db 数量是否有性能/管理上限需要确认（issue 阶段验证）。预期数千 workspace 量级 OK，更高需要 sharding。
- **schema 变更分发**：业务表 schema 升级时，所有 ws db 都要跑迁移。需要一个迁移 runner（启动时遍历 `_system.workspace` → 逐 db 应用 schema 增量）。
- **DDL 权限的精确表达**：SurrealDB 是否允许 record-style user 直接执行 DDL 需要实测；如不允许，需要在 §2 落地 admin access 双签方案。
- **跨 db 报表**（如"这个 OIDC 用户全部 workspace 的合计 token 用量"）需要在后端内做 fan-out 查询，不能一句 SQL 搞定。MVP 不做这种报表，长期接受复杂度。
- **execTemplate 仍是关键密钥持有者**：root 凭证的保管和最小化使用是 launch-blocker 工程项。
- **一个真人在多个 workspace 中是多条 user 记录**：用户改 display_name / avatar 时要 fan-out 到所有 workspace db。MVP 接受手动同步或在 `_system` 加一个 `user_profile` 缓存表。

## Alternatives Considered

### A. 单 db + 复杂 PERMISSIONS（之前几稿）

抛弃。理由：PERMISSIONS 嵌套深、跨 workspace 隔离全靠人工写表达式、service JWT 攻击面大、DDL 必须中转。

### B. 每 workspace 一个 namespace（而不是 db）

抛弃。理由：namespace 在 SurrealDB 中更接近"租户隔离"，但 namespace 之间无法共享任何东西；本架构需要 `_system` 来索引所有 workspace + 承载 pending 邀请，namespace 模型让 `_system` 和 ws_* 之间彻底无法跨。db 模型在同 namespace 下更易管理。

### C. 用户表全局存在 _system

抛弃。理由：违背"db 是隔离单元"的核心原则；任何 workspace 内的 PERMISSIONS 想引用 user 都必须跨 db；且用户在不同 workspace 中可能有不同 display_name / role / 偏好，全局表反而造成数据耦合。

### D. 虚拟员工用每员工一把长期 JWT（早期稿）

抛弃。RECORD access + 短期 SIGNIN 替代之，无长期凭证保管负担。

## Open Questions

1. **schema 迁移 runner**：MVP 是启动时遍历，还是后台定时？倾向启动时遍历 + 失败显式上报。
2. **OIDC provider 选择**：本 ADR 不指定。但 `DEFINE ACCESS admin ... URL <jwks>` 强制选定一个 IdP，需要在 issue 阶段定。
3. **drop database**（退休 workspace）：MVP 不提供，仅支持"归档"标记；真正 drop 留待数据合规章节正式立项。
4. **participant access 的 SIGNIN 凭据**：当前设计是直接拿 OIDC subject 作为 SIGNIN 入参；这要求 SIGNIN 调用本身**只在后端进程内**发起（因为后端已经 OIDC verify 过）。前端永远不直连 SurrealDB（已由 [`web-only-pivot.md`](./web-only-pivot.md) 保证）。如果未来出现"前端直连"需求，要把 participant access 改为 JWT type 并增加 AUTHENTICATE 校验 `is_admin=false`——记为升级点。
5. **同 OIDC subject 在 admin 与 participant access 间的切换**：用户从普通成员被提升为管理员后，已签发的 participant token 仍在 1h 内有效。MVP 接受这一延迟；高敏环境可以缩短 DURATION 或加 token blacklist。
